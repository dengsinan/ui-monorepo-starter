import type { ThemeConfig } from 'antd';
import { theme } from 'antd';

const defaultThemeDark: ThemeConfig = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: '#FF1654',
  },
};

export default defaultThemeDark;
